export interface WaitlistFormData {
  name: string;
  email: string;
  message?: string;
}

export interface ContactFormData {
  name: string;
  email: string;
  subject: string;
  message: string;
}
